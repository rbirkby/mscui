<UserControl x:Class="Microsoft.Cui.SamplePages.ActivityDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"              
    >
    <Grid x:Name="LayoutRoot">
        <Grid.Resources>
            <Style x:Key="LabelStyle" TargetType="ContentControl">
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Margin" Value="0,0,0,10" />
                <Setter Property="Padding" Value="0,0,10,0" />
            </Style>
        </Grid.Resources>
        <Grid Margin="10" x:Name="Dialog">            
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="DialogTitle" Text="Administration" Margin="0,0,0,10"  Style="{StaticResource TextBlockStyle}" FontSize="16" Opacity="0.5" />

           
            <Button x:Name="EventsCloseButton" Content="Close" IsTabStop="False"
                    Grid.Row="6" HorizontalAlignment="Right" 
                    Style="{StaticResource BasicButtonStyle}" Click="Button_Click"/>
        </Grid>
    </Grid>
</UserControl>
